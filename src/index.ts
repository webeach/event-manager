/**
 * event-manager — a library for simplifying event handling in JavaScript.
 *
 * @module event-manager
 */

export { listen } from './functions/listen';

export type { EventManager } from './modules/EventManager';
